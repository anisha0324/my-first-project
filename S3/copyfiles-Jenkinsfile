pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh '/usr/local/bin/aws s3 cp --recursive /Users/nareshsalla/Documents/assignment/ s3://test2ani-bucket/application/'

            }
        }
    }
}